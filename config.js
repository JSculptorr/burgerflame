/* ============================================================
   BURGER FLAME — Конфигурация Telegram-бота
   ============================================================

   📌 КАК НАСТРОИТЬ TELEGRAM-БОТА (3 простых шага):

   ─────────────────────────────────────────────
   ШАГ 1. Создайте бота через @BotFather
   ─────────────────────────────────────────────
   1) Откройте Telegram и найдите @BotFather
   2) Отправьте команду /newbot
   3) Придумайте имя боту (например: BurgerFlame Orders)
   4) Придумайте username (должен заканчиваться на "bot",
      например: burgerflame_orders_bot)
   5) BotFather пришлёт TOKEN вида:
      123456789:AAH-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
      → Скопируйте этот токен в BOT_TOKEN ниже.

   ─────────────────────────────────────────────
   ШАГ 2. Получите свой CHAT_ID
   ─────────────────────────────────────────────
   ВАРИАНТ А (личные сообщения):
   1) Найдите бота @userinfobot в Telegram
   2) Отправьте ему /start
   3) Он пришлёт ваш ID — это и есть CHAT_ID
   4) ⚠️ ВАЖНО: после этого напишите своему новому боту
      любое сообщение (например /start), иначе он не сможет
      вам писать.

   ВАРИАНТ Б (группа/канал — для команды кафе):
   1) Создайте группу в Telegram, добавьте туда своего бота
      и сделайте его администратором
   2) Отправьте в группе любое сообщение
   3) Откройте в браузере:
      https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates
      (вместо <ВАШ_ТОКЕН> вставьте токен из ШАГА 1)
   4) Найдите в ответе "chat":{"id":-1001234567890,...}
      → Это и есть CHAT_ID (для групп он со знаком минус).

   ─────────────────────────────────────────────
   ШАГ 3. Вставьте значения ниже и сохраните файл
   ─────────────────────────────────────────────
   После этого перезапустите сервер: npm start

   💡 АЛЬТЕРНАТИВА: вместо редактирования этого файла можно
   создать файл .env в корне проекта (см. .env.example) и
   указать там TELEGRAM_BOT_TOKEN и TELEGRAM_CHAT_ID — они
   будут иметь приоритет над значениями отсюда.
   ============================================================ */

module.exports = {
    // 🔑 Токен бота от @BotFather
    BOT_TOKEN: '',

    // 💬 ID чата, куда будут приходить заказы
    CHAT_ID: '',

    // 🌐 Порт, на котором запускается сервер
    PORT: 3000
};
